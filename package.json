{
  "name": "autorite-backend",
  "version": "1.0.0",
  "description": "AutoRite Backend",
  "main": "index.js",
  "scripts": {
    "start": "export NODE_ENV=test && nodemon server.js",
    "development": "export NODE_ENV=development && node server.js",
    "production": "export NODE_ENV=production && node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "config": "^3.3.8",
    "express": "^4.18.1",
    "http-status": "^1.5.3",
    "mysql": "^2.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
